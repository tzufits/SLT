@if(Request.IsAuthenticated) {
    <text>
        [ שלום <strong>@User.Identity.Name</strong>! [ @Html.ActionLink("התנתק", "LogOff", "Account") 
    </text>
}
else {
    @:[ @Html.ActionLink("התחבר", "LogOn", "Account") ]
}
